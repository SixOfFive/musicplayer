# Kept intentionally empty. Native module rebuilds are handled by the
# `postinstall` script in package.json (electron-builder install-app-deps,
# which invokes @electron/rebuild under the hood). See README.

node_modules
dist
dist-electron
release
out
.vscode
.claude
*.log
.DS_Store
.env
.env.local
# electron-builder cache
.eb-cache
# TypeScript incremental build info
*.tsbuildinfo
# vite.config compiled output (we commit the .ts only)
vite.config.d.ts
vite.config.js
# Assistant scratch files — never publish. Contains local creds + notes.
BRAINDUMP.md
brainstart.md
